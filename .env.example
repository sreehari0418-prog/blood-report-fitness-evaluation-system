# Firebase Project Configuration Template
# Copy this to .env and fill in your Firebase project credentials

# Get these values from Firebase Console:
# 1. Go to https://console.firebase.google.com/
# 2. Select your project (or create new one)
# 3. Click "Project Settings" (gear icon)
# 4. Scroll down to "Your apps" section
# 5. Click "Web" icon for Firebase config
# 6. Copy the values below

VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=1:your-app-id:web:your-app-code

# ── AWS Textract Configuration (for accurate PDF blood report extraction) ──
# Get these from: AWS Console → IAM → Users → Your User → Security Credentials
# Required IAM Policy: AmazonTextractFullAccess
#
# Steps:
# 1. Go to https://aws.amazon.com → Sign in → IAM → Users
# 2. Create user (or select existing) → Attach policy: AmazonTextractFullAccess
# 3. Security credentials tab → Create access key → Copy values below
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
